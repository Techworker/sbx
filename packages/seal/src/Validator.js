class Validator
{
  constructor()
  {

  }

  /**
   * Gets a value indicating whether the given operation of the given account is valid
   * by recalculating the seal and comparing it.
   *
   * @param {Account} account
   * @param {Operation} operation
   * @param {Operation[]} gapOperations
   */
  validate(account, operation, gapOperations) {

  }
}
